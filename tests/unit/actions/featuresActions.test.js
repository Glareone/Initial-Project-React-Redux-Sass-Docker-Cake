describe('Features Actions', () => {
  describe('selectFeatures', () => {
    const someSortOfData = 1;
    it('for CI/CD TESTING PURPOSES', () => {
      expect(someSortOfData).toEqual(1);
    });
  });
});
